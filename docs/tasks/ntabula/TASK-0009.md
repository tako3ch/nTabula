# TASK-0009: nTabulaTests ターゲット + MockURLProtocol 作成

**タスクID**: TASK-0009
**タスクタイプ**: DIRECT
**推定工数**: 6時間
**フェーズ**: Phase 4 - テスト基盤
**信頼性レベル**: 🔵 *NFR-303・architecture.md テスト注入設計・design-interview.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/ntabula/requirements.md) - NFR-301, NFR-302, NFR-303
- **設計文書**: [📐 architecture.md](../../design/ntabula/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/ntabula/dataflow.md) - フロー11
- **型定義**: [📝 interfaces.swift](../../design/ntabula/interfaces.swift)

## タスク概要

Xcode プロジェクトに `nTabulaTests` ターゲットを追加し、MockURLProtocol クラスを実装する。このターゲットが TASK-0010（PersistenceManager テスト）と TASK-0011（NotionService テスト）の基盤になる。

## 依存タスク

- **前提タスク**: なし（Phase 1〜3 と並行して準備可能）
- **後続タスク**: [TASK-0010: PersistenceManager テスト実装](TASK-0010.md), [TASK-0011: NotionService テスト実装](TASK-0011.md)

## 完了条件

- [ ] Xcode に `nTabulaTests` ターゲットが追加されている
- [ ] `nTabulaTests/Mocks/MockURLProtocol.swift` が作成されている
- [ ] MockURLProtocol の `requestHandler` クロージャでレスポンスを制御できる
- [ ] `@testable import nTabula` でビルドが通る
- [ ] テストターゲットで空のテストが実行できる（green）

---

## 実装詳細

### 1. nTabulaTests ターゲット追加（Xcode操作） 🔵

**信頼性**: 🔵 *NFR-303 テストターゲット設計より*

Xcode での操作:
1. File → New → Target → Unit Testing Bundle
2. Product Name: `nTabulaTests`
3. Bundle ID: `jp.umi.design.nTabulaTests`
4. Target to be Tested: `nTabula`

**実装ファイル**: `nTabula.xcodeproj/project.pbxproj`（Xcode が自動更新）

### 2. MockURLProtocol 実装 🔵

**信頼性**: 🔵 *interfaces.swift MockURLProtocol・dataflow.md フロー11より*

**実装ファイル**: `nTabulaTests/Mocks/MockURLProtocol.swift`

```swift
import Foundation

/// URLSession をモックするための URLProtocol サブクラス（NFR-301）
class MockURLProtocol: URLProtocol {

    /// テスト用のレスポンスをクロージャで設定
    static var requestHandler: ((URLRequest) throws -> (HTTPURLResponse, Data))?

    override class func canInit(with request: URLRequest) -> Bool {
        return true
    }

    override class func canonicalRequest(for request: URLRequest) -> URLRequest {
        return request
    }

    override func startLoading() {
        guard let handler = MockURLProtocol.requestHandler else {
            client?.urlProtocolDidFinishLoading(self)
            return
        }

        do {
            let (response, data) = try handler(request)
            client?.urlProtocol(self, didReceive: response, cacheStoragePolicy: .notAllowed)
            client?.urlProtocol(self, didLoad: data)
            client?.urlProtocolDidFinishLoading(self)
        } catch {
            client?.urlProtocol(self, didFailWithError: error)
        }
    }

    override func stopLoading() {}
}
```

### 3. テスト用 URLSession ヘルパー 🔵

**信頼性**: 🔵 *dataflow.md フロー11 MockURLProtocol使用例より*

**実装ファイル**: `nTabulaTests/Helpers/TestHelpers.swift`

```swift
import Foundation

extension URLSession {
    /// MockURLProtocol を使用するテスト用 URLSession を生成
    static func makeMockSession() -> URLSession {
        let config = URLSessionConfiguration.ephemeral
        config.protocolClasses = [MockURLProtocol.self]
        return URLSession(configuration: config)
    }
}
```

---

## 動作確認要件

### 確認ケース1: テストターゲットのビルド 🔵

**信頼性**: 🔵 *NFR-303より*

- `nTabulaTests` ターゲットが Xcode でビルドできる
- `@testable import nTabula` でビルドエラーなし

### 確認ケース2: MockURLProtocol の動作 🔵

**信頼性**: 🔵 *dataflow.md フロー11より*

- MockURLProtocol.requestHandler にモックレスポンスを設定
- URLSession.makeMockSession() でリクエストを送ると requestHandler が呼ばれる

---

## 実装手順

1. `/tsumiki:direct-setup` - Xcode でテストターゲット追加 + MockURLProtocol 実装
2. `/tsumiki:direct-verify` - ビルド確認・空テスト実行確認

---

## 注意事項

- `@testable import nTabula` が動作するように `nTabula` ターゲットの `SWIFT_COMPILATION_MODE` を確認
- MockURLProtocol.requestHandler はテスト毎に reset が必要（afterEach で nil に設定）
- nTabulaTests ターゲットの iOS/macOS 設定は `nTabula` ターゲットと同じ macOS 14.0 にする

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 動作確認 | 2 | 0 | 0 | 2 |

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)

**品質評価**: ✅ 高品質
